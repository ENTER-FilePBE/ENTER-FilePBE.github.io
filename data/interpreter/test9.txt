partition-by extension
  if = extension ConstStr(sh)
    then
      transform
        setExecutable(true)
    else
      if = extension ConstStr(sh)
        then
          transform
            setPath(extension)
        else
          transform
            doNothing
